---
title: "缓存一致性——数据库与缓存的最终一致性方案对比"
date: 2026-06-28
description: 从经典的 Cache Aside 到 Write Behind，拆解四种缓存一致性模式的原理、利弊和代码实践，用 Mermaid 时序图还原 Cache Aside 并发下的隐蔽 Bug。
tags: ["分布式","缓存","Redis","一致性","Cache Aside","性能优化"]
categories: ["分布式系统"]
---

```mermaid
flowchart LR
    A["📖 读请求"] --> B["Cache Aside\n旁路缓存，最常用"]
    C["📝 写请求"] --> D{"更新策略？"}
    D -->|"先删缓存再写DB"| E["❌ 有并发风险"]
    D -->|"先写DB再删缓存"| F["✅ 主流做法\n短暂不一致"]
    D -->|"双写+MQ"| G["✅ 最终一致\n实现复杂"]
    D -->|"旁路+过期"| H["⚡ 最简单\n允许短暂不一致"]
```

---

# 一、为什么有缓存一致性这个问题？

**问题的本质**：一份数据存了两个地方（数据库 + 缓存），对它们的写操作不是原子的。

```
理想：DB 和 Cache 始终一致
现实：任何先写一个后写另一个的方案，中间都有一段时间不一致
```

**工程态度**：接受短暂不一致，保证最终一致。

---

# 二、Cache Aside（旁路缓存）——最主流方案

## 2.1 读流程

```
读请求 → 查缓存 → 命中：返回
                 → 未命中：查 DB → 写入缓存 → 返回
```

## 2.2 写流程——为什么先删缓存后写 DB 有 Bug？

```mermaid
sequenceDiagram
    participant C1 as 客户端 A（写）
    participant C2 as 客户端 B（读）
    participant Cache as 缓存
    participant DB as 数据库
    
    C1->>Cache: ① 删除缓存
    C1->>DB: ② 开始写 DB（未完成）
    
    C2->>Cache: ③ 查缓存（miss）
    C2->>DB: ④ 查 DB（读到旧值）
    C2->>Cache: ⑤ 旧值写入缓存！
    
    C1->>DB: ⑥ DB 写入完成
    
    Note over Cache,DB: 缓存中是旧值，DB 是新值 → 不一致！
```

**正确的写法：先更新 DB，再删除缓存。**

```mermaid
sequenceDiagram
    participant C1 as 客户端 A（写）
    participant DB as 数据库
    participant Cache as 缓存
    
    C1->>DB: ① 更新数据库
    C1->>Cache: ② 删除缓存
    
    Note over Cache,DB: 这中间如果有人读：DB 新值 → 写回缓存 ✅
```

## 2.3 删除缓存失败的兜底——延迟双删

```java
public void updateWithRetry(String key, Object value) {
    db.update(value);
    redis.del(key);
    
    // 异步延迟再删一次（兜底删除失败）
    scheduler.schedule(() -> {
        redis.del(key);
    }, 500, TimeUnit.MILLISECONDS);
}
```

**更可靠的方案：订阅 MySQL binlog，异步更新/删除缓存。**

---

# 三、Read/Write Through（缓存代理模式）

缓存层承担"数据库代理"角色，业务只和缓存打交道：

```mermaid
flowchart LR
    APP["业务代码"] --> CACHE["缓存层\n(代理)"]
    CACHE --> DB["数据库"]
    
    CACHE -->|"Read Through\ncache miss → 缓存去查DB"| DB
    CACHE -->|"Write Through\n同步写缓存+DB"| DB
```

**优点**：业务代码简单。**缺点**：缓存层承担了同步写 DB 的压力，写入延迟高。

---

# 四、Write Behind（异步回写）

```mermaid
flowchart LR
    APP["业务代码"] --> CACHE["缓存\n(只写缓存)"]
    CACHE -.->|"异步批量\n刷回DB"| DB["数据库"]
```

**优点**：写入极快（只写缓存）。**缺点**：缓存挂了数据全丢。仅适合非关键数据（如浏览量计数）。

---

# 五、四种方案对比

| 方案 | 写延迟 | 一致性 | 复杂度 | 适用场景 |
|------|--------|--------|--------|----------|
| **Cache Aside** | 低 | 最终一致 | ⭐⭐ | 通用场景，90% 首选 |
| **Read/Write Through** | 中 | 强/最终 | ⭐⭐⭐ | 缓存中间件（如 Redis + 自定义代理） |
| **Write Behind** | 极低 | 弱 | ⭐⭐⭐ | 非关键数据（计数器） |
| **Binlog 订阅** | 低 | 最终一致 | ⭐⭐⭐⭐ | 对一致性要求高 |

---

# 六、总结

> **Cache Aside + 先写 DB 再删缓存 + Binlog 兜底**是业界标配。缓存一致性没有银弹，只有根据业务接受的不一致窗口选择合适的兜底策略。
